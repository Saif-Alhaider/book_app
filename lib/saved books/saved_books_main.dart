import 'package:flutter/material.dart';

class SavedBooksMain extends StatelessWidget {
  const SavedBooksMain({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}